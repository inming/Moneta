# 统计报表 — 功能规格归档
> 从 PRD.md Section 3.4 归档。此功能已实现。

### 3.4 统计报表

统计报表作为独立页面，在侧栏中新增「统计报表」入口（位于「数据浏览」和「设置」之间）。页面整体分为上下两部分：上方为筛选器 + 汇总卡片区，下方为交叉统计表和辅助图表。

#### 3.4.0 导航变更

侧栏新增一项：

| 菜单项 | 路由 | 图标 | 说明 |
|--------|------|------|------|
| 统计报表 | `/statistics` | BarChartOutlined | 交叉统计 + 图表 |

侧栏顺序调整为：数据浏览 → **统计报表** → 设置 → 锁屏（底部）。

#### 3.4.1 筛选器与汇总卡片

##### 筛选器

页面顶部提供一行筛选控件，所有筛选条件变化时实时刷新下方所有区域（交叉统计表 + 图表 + 汇总卡片）：

| 筛选项 | 控件 | 默认值 | 说明 |
|--------|------|--------|------|
| 年份 | 年份选择器 | 当前年份 | 范围：数据库中最早交易年份 ~ 当前年份；无数据时仅显示当前年份；年度汇总视图下隐藏 |
| 类型 | 单选按钮组 | 消费 | 消费 / 收入 / 投资，三选一 |

- 筛选器状态不持久化，每次进入页面恢复默认值
- 类型为单选（交叉统计表同一时间只展示一种类型的分类体系）

##### 汇总卡片

筛选器下方展示一行汇总卡片（3 张），数据与当前筛选条件联动：

| 卡片 | 内容 | 说明 |
|------|------|------|
| 本月合计 | 当前年份 + 当前月份 + 当前类型的金额总计 | 若当前年份非今年，显示该年 12 月的数据 |
| 上月合计 | 上一个月同类型金额总计 | 与本月卡片的月份联动 |
| 年度合计 | 当前年份 + 当前类型的全年金额总计 | 即交叉统计表「合计」列的总计行值 |

- 金额显示使用千分号格式（与数据浏览页一致）
- 卡片下方显示环比变化（本月 vs 上月），正数显示为红色上箭头，负数显示为绿色下箭头，零不显示箭头
- 「本月」的定义：若筛选年份 = 当前年份，则取系统当前月份；若筛选年份为历史年份，则取 12 月

#### 3.4.2 交叉统计表（核心视图）

对标用户现有 Excel 透视表，以 Ant Design Table 实现。

##### 表结构

| 列 | 说明 | 宽度 |
|----|------|------|
| 分类 | 分类名称，固定在左侧 | 120px，固定列（`fixed: 'left'`） |
| 1月 ~ 12月 | 各月金额合计 | 等宽，可横向滚动 |
| 合计 | 全年金额总计 | 固定列（`fixed: 'right'`） |

- **行**：当前类型下所有**有数据的**分类，按分类排序顺序（`sort_order`）排列
- **末行**：总计行（每列求和），字体加粗
- **值**：金额显示千分号 + 两位小数；金额为 0 时显示 `—`（em dash），避免数字噪音
- **分类行只展示有数据的分类**：若某分类全年 12 个月金额均为 0，该行不显示（避免空行过多）

##### 数据查询

- 后端通过 `db:stats:cross-table` IPC 通道查询
- SQL 按 `(category_id, month)` 分组聚合，一次查询返回全年数据
- 年度汇总由后端计算后返回（非前端二次聚合），减少前端计算复杂度

##### 交互

- 表格支持横向滚动（月份列较多时），分类列和合计列固定
- 无分页（分类数量有限，通常 < 30 行）
- 不支持行内编辑（只读视图）

##### 空状态

- 当前筛选条件下无任何交易数据时，显示空状态占位图 + 提示文案「暂无数据，请先录入交易记录」

#### 3.4.3 趋势图表

统计表下方展示趋势柱状图，使用 ECharts 实现，与筛选器联动。月度明细和年度汇总各自有对应的趋势图。

##### 月度趋势柱状图

| 属性 | 说明 |
|------|------|
| 数据源 | 交叉统计表各分类的 12 个月金额 |
| X 轴 | 1月 ~ 12月 |
| Y 轴 | 金额（自动刻度，≥1万时显示「x.x万」） |
| 展示 | 堆叠柱状图，每个分类一个系列 |
| 交互 | hover tooltip 只显示鼠标悬停的数据块（单个分类），显示分类名称和金额；图例单击独显/恢复 |

##### 年度趋势柱状图

| 属性 | 说明 |
|------|------|
| 数据源 | 年度汇总表各分类的各年度金额 |
| X 轴 | 年份（如 2024年、2025年） |
| Y 轴 | 金额（自动刻度，≥1万时显示「x.x万」） |
| 展示 | 堆叠柱状图，每个分类一个系列 |
| 交互 | hover tooltip 只显示鼠标悬停的数据块（单个分类）；与月度趋势图一致 |

- 图表高度固定 380px
- 跨年无数据的分类（合计为 0）不生成系列
- 无数据时显示空状态提示

##### 图表数据下钻（查看明细）

趋势柱状图支持右键点击数据块，快速跳转至数据浏览页查看该数据块对应的明细交易记录。

**触发方式**
- 在月度趋势图或年度趋势图的任意数据块上**右键单击**
- 弹出上下文菜单，显示「查看明细」选项

**跳转逻辑**
点击「查看明细」后，应用自动导航至数据浏览页（`/`），并根据当前图表上下文自动设置以下筛选条件：

| 视图 | 自动设置的筛选条件 |
|------|-------------------|
| **月度趋势图** | 日期范围 = 该月份全月（如 2024-10-01 ~ 2024-10-31）<br>分类 = 右键点击的分类<br>类型 = 当前统计报表选中的类型（消费/收入/投资） |
| **年度趋势图** | 日期范围 = 该年份全年（如 2024-01-01 ~ 2024-12-31）<br>分类 = 右键点击的分类<br>类型 = 当前统计报表选中的类型（消费/收入/投资） |

**状态传递**
- 通过 URL query parameters 传递筛选状态（如 `/?dateFrom=2024-10-01&dateTo=2024-10-31&category_id=5&type=expense`）
- 数据浏览页解析 URL 参数，自动应用筛选条件并加载数据
- 用户可在数据浏览页手动清除或修改筛选条件

**来源标记与状态保留**
- 跳转时 URL 追加来源标记 `from=statistics`，标识是从统计页跳转而来
- 追加统计页当前状态参数：`year`（年份）、`tab`（视图Tab：monthly/yearly）、`soloCategory`（独显的分类，可选）
- 完整 URL 示例：`/?dateFrom=2024-10-01&dateTo=2024-10-31&category_id=5&type=expense&from=statistics&year=2024&tab=monthly&soloCategory=正餐`
- 数据浏览页检测到 `from=statistics` 时显示「← 返回统计页」按钮
- 返回按钮点击后导航至 `/statistics?year=2024&tab=monthly&soloCategory=正餐`，恢复统计页完整状态

**边界处理**
- 点击的块金额为 0 时：仍允许查看明细（结果为空）
- 分类为「合计」列（如点击总计行）：不设置分类筛选，仅按日期范围筛选
- 导航前若数据浏览页有未保存的编辑/新增行：提示用户保存或放弃

#### 3.4.4 年度分类汇总表

在现有交叉统计表（分类 × 月份）之上，新增「年度分类汇总表」视图，从跨年维度展示各分类的金额变化趋势。两个视图通过页面顶部的 Radio.Group 切换，共享类型筛选器。

##### 入口与切换

- 页面最顶部通过 `Radio.Group`（按钮样式）切换：「月度明细」（现有交叉统计表） | 「年度汇总」（本表）
- 默认展示「月度明细」Tab
- Tab 切换时不影响类型筛选器状态，但「年度汇总」Tab 下**隐藏年份选择器**（因为该表本身展示所有年份）
- 月度明细下方显示月度趋势柱状图，年度汇总下方显示年度趋势柱状图

##### 表结构

| 列 | 说明 | 宽度 |
|----|------|------|
| 年度 | 年份（如 2024、2025），固定在左侧 | 80px，固定列（`fixed: 'left'`） |
| 分类1 ~ 分类N | 当前类型下有数据的各分类 | 等宽，可横向滚动 |
| 合计 | 该年度所有分类金额总计 | 固定列（`fixed: 'right'`） |

- **行**：数据库中有交易数据的所有年份，按年份**升序**排列（从最早到最近）
- **列**：当前类型下所有**跨年有数据的**分类，按分类排序顺序（`sort_order`）排列
- **末行**：总计行（每列求和），字体加粗
- **值**：金额显示千分号 + 两位小数；金额为 0 时显示 `—`（em dash）
- **分类列只展示有数据的分类**：若某分类在所有年份中金额均为 0，该列不显示

##### 数据查询

- 后端通过新增 IPC 通道 `db:stats:yearly-category` 查询
- SQL 按 `(year, category_id)` 分组聚合，一次查询返回所有年份数据
- 筛选参数：`type`（必填）
- 年度合计和分类合计均由后端计算后返回

##### 年度趋势柱状图

- 年度汇总表下方展示堆叠柱状图，X 轴为年份，每个分类一个堆叠系列
- 交互逻辑与月度趋势柱状图一致：tooltip 排序合计、图例单击独显/恢复
- 无数据时显示空状态

##### 交互

- 表格支持横向滚动（分类列较多时），年度列和合计列固定
- 无分页（年份数量有限，通常 < 10 行）
- 不支持行内编辑（只读视图）

##### 空状态

- 当前筛选条件下无任何交易数据时，显示空状态占位图 + 提示文案「暂无数据」

#### 3.4.5 验收标准

##### 导航与页面结构

| # | 验收条件 | 说明 |
|---|---------|------|
| AC-S1 | 侧栏新增「统计报表」入口 | 位于数据浏览和设置之间 |
| AC-S2 | 点击进入统计报表页面 | 路由 `/statistics` |
| AC-S3 | 页面包含筛选器、汇总卡片、交叉统计表、辅助图表四个区域 | 从上到下依次排列 |

##### 筛选器

| # | 验收条件 | 说明 |
|---|---------|------|
| AC-S4 | 年份选择器默认当前年份 | 范围覆盖数据中最早年份到当前年份 |
| AC-S5 | 类型单选默认「消费」 | 三选一，切换后所有区域刷新 |
| AC-S6 | ~~操作人下拉~~ | 已移除 |
| AC-S7 | 筛选条件变化时实时刷新 | 无需手动点击刷新按钮 |

##### 汇总卡片

| # | 验收条件 | 说明 |
|---|---------|------|
| AC-S8 | 显示本月合计、上月合计、年度合计 | 三张卡片一行排列 |
| AC-S9 | 金额使用千分号格式 | 与数据浏览页一致 |
| AC-S10 | 本月 vs 上月显示环比变化 | 正数红色上箭头，负数绿色下箭头 |

##### 交叉统计表

| # | 验收条件 | 说明 |
|---|---------|------|
| AC-S11 | 行为当前类型下有数据的分类 | 全年金额为 0 的分类不显示 |
| AC-S12 | 列为 12 个月 + 年度合计 | 共 13 列 + 分类列 |
| AC-S13 | 分类列固定左侧，合计列固定右侧 | 横向滚动时不移动 |
| AC-S14 | 底部总计行字体加粗 | 每列金额为各分类之和 |
| AC-S15 | 金额为 0 显示 em dash（—） | 避免数字噪音 |
| AC-S16 | 金额使用千分号 + 两位小数 | 如 1,234.56 |
| AC-S17 | 无数据时显示空状态 | 提示文案引导用户录入 |

##### 趋势图表

| # | 验收条件 | 说明 |
|---|---------|------|
| AC-S18 | ~~饼图~~ | 已移除 |
| AC-S19 | ~~饼图合并~~ | 已移除 |
| AC-S20 | 月度明细下方显示堆叠柱状图 | X 轴为月份，每个分类一个堆叠系列 |
| AC-S21 | 图例单击独显/恢复 | 再次点击已独显的分类恢复全部显示 |
| AC-S49 | hover tooltip 只显示单个分类 | 鼠标悬停在数据块上时，tooltip 仅显示该分类名称和金额，不显示其他分类 |
| AC-S22 | 图表与筛选器联动 | 切换年份/类型后图表刷新 |
| AC-S23 | 无数据时图表显示空状态 | 不显示空坐标轴 |
| AC-S35 | 趋势图支持右键查看明细 | 右键点击数据块弹出上下文菜单 |
| AC-S36 | 上下文菜单显示「查看明细」选项 | 菜单项文字清晰可辨 |
| AC-S37 | 点击后跳转至数据浏览页 | 路由切换至 `/` |
| AC-S38 | 自动设置日期范围筛选 | 月度图 = 该月全月，年度图 = 该年全年 |
| AC-S39 | 自动设置分类和类型筛选 | 分类 = 点击的分类，类型 = 当前选中类型 |
| AC-S40 | 筛选状态通过 URL 传递 | 刷新页面后筛选条件不丢失 |
| AC-S41 | 金额为 0 的块也支持下钻 | 结果为空时显示空状态 |
| AC-S42 | 数据浏览页正确解析 URL 参数 | 自动应用筛选并加载数据 |
| AC-S43 | 跳转 URL 包含来源标记 `from=statistics` | 标识是从统计页跳转而来 |
| AC-S44 | 跳转 URL 包含统计页状态参数 | year、tab、可选的 soloCategory |
| AC-S45 | 数据浏览页根据来源标记显示返回按钮 | 检测 `from=statistics` 显示返回按钮 |
| AC-S46 | 返回按钮点击后恢复统计页状态 | 年份、类型、视图Tab、独显分类均恢复 |
| AC-S47 | 直接访问数据浏览页不显示返回按钮 | 无 `from=statistics` 参数时不显示 |
| AC-S48 | 返回按钮样式符合设计规范 | 左箭头图标 + 「返回统计页」文字 |

##### 年度分类汇总表

| # | 验收条件 | 说明 |
|---|---------|------|
| AC-S24 | 页面顶部 Radio.Group 提供「月度明细」和「年度汇总」切换 | 默认显示「月度明细」 |
| AC-S25 | 切换至「年度汇总」时隐藏年份选择器 | 年度汇总本身展示所有年份，无需指定年份 |
| AC-S26 | 月度明细下方显示月度趋势图，年度汇总下方显示年度趋势图 | 各自独立的堆叠柱状图 |
| AC-S27 | 行为数据库中有数据的所有年份 | 按年份升序排列 |
| AC-S28 | 列为当前类型下跨年有数据的分类 | 所有年份金额均为 0 的分类不显示 |
| AC-S29 | 年度列固定左侧，合计列固定右侧 | 横向滚动时不移动 |
| AC-S30 | 底部总计行字体加粗 | 每列金额为各分类之和 |
| AC-S31 | 金额为 0 显示 em dash（—） | 与月度明细一致 |
| AC-S32 | 金额使用千分号 + 两位小数 | 与月度明细一致 |
| AC-S33 | 类型筛选器与年度汇总表联动 | 切换后表格和图表数据刷新 |
| AC-S34 | 无数据时显示空状态 | 表格和图表均显示空状态 |
